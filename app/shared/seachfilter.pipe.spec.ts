import { SeachfilterPipe } from './seachfilter.pipe';

describe('SeachfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SeachfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
